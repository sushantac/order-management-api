# NEVER commit real secrets. This is the DEV-only, plaintext shape that the
# Bitnami Sealed Secrets controller encrypts into the sealed manifest (below).
# In the real flow: kubectl create secret generic order-api-secrets --dry-run=client -o yaml \
#   | kubeseal --format yaml > k8s/overlays/prod/sealed-db-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: order-api-secrets
type: Opaque
data:
  DB_USER: b3JkZXI=        # order (base64)
  DB_PASSWORD: b3JkZXI=    # order (base64 - DEV ONLY)
