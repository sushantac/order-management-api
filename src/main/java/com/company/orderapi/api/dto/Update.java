package com.company.orderapi.api.dto;

/**
 * PR #23 - validation group for UPDATE operations. Update rules are usually
 * more lenient (a full create-time invariant may already be satisfied).
 */
public interface Update {
}
